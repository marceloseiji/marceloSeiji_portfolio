//lastPageScroll = 0;

//$(document).scroll(function(){
   // scr = $(this).scrollTop();

   // if (scr > lastPageScroll) {
    //    $("#imgMarcelo").css("top", 100 + scr);
   // } else {
  //  };

//});